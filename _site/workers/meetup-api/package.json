{
  "name": "meetup-api-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate": "wrangler d1 migrations apply meetup_db",
    "d1:seed": "wrangler d1 execute meetup_db --file=./migrations/0001_schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.7.0"
  }
}
