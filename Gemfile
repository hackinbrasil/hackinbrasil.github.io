source "https://rubygems.org"

ruby "~> 2.6.10"

gem "github-pages", group: :jekyll_plugins

# `ffi` 1.17+ requires Ruby >= 3.0. Pin to keep Ruby 2.6 compatibility.
gem "ffi", "< 1.17"

# Needed for local `bundle exec jekyll serve`
gem "webrick", "~> 1.8"
